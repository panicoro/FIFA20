from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Predictions of players positions based on their features.',
    author='Pablo Rosa',
    license='MIT',
)
